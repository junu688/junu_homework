### 1-19

```js
var a;
console.log(a); // 선언만 하고 값 할당 x

var obj = { a: 1 };
console.log(obj.a); //  존재하는 프로퍼티
console.log(obj.b); //  존재하지 않는 프로퍼티에 접근
console.log(b); // c.f) ReferenceError: b is not defined

var func = function() {};
var c = func(); // return 값이 없으면 undefined를 반환한 것으로 간주.
console.log(c); // undefined
```