### 1-12

```js
var copyObject = function(target) {
  var result = {}; // 새로운 빈 객체 생성
  for (var prop in target) { // target의 모든 프로퍼티 복사
    result[prop] = target[prop]; // 복사됨
  }
  return result;
};
```